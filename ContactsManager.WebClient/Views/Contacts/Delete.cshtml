@model ContactsManager.WebClient.ViewModel.Contact.EmployeeViewModel

@{
    ViewBag.Title = "DeleteContact";
}

<h2>Delete Contact</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Contact</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PersonFullName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AnyDesk)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Branch)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.JobTitle)
        </dd>
    </dl>

    @using (Html.BeginForm("Delete", "Contacts")) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "ContactsView")
        </div>
    }
</div>
